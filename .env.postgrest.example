# =============================================================================
# DIRECTUS API Configuration
# =============================================================================
# These environment variables should be set in Netlify for production deployments.
# For local development, copy this file to .env and fill in the values.

# Directus API URL (your Directus instance)
VITE_DIRECTUS_URL=https://sql.traidenis.org

# Directus Static Token (create in Directus Admin > Users > your API user)
# This token authenticates all API requests from the frontend.
# Set this in Netlify Environment Variables for production.
VITE_DIRECTUS_TOKEN=your_directus_static_token_here

# Anthropic SDK (for AI features)
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key

# =============================================================================
# LEGACY: PostgreSQL + PostgREST Configuration (no longer used)
# The application now uses Directus API for all data queries.
# =============================================================================
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_secure_password_here
# POSTGRES_DB=postgres
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGREST_URL=http://localhost:3000
# POSTGREST_PASSWORD=your_authenticator_password
# POSTGREST_JWT_SECRET=your_jwt_secret_minimum_32_characters_long_change_this
# VITE_POSTGREST_URL=http://localhost:3000
# VITE_POSTGREST_ANON_KEY=anon
