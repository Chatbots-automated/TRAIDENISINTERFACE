# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# PostgREST Configuration
POSTGREST_URL=http://localhost:3000
POSTGREST_PASSWORD=your_authenticator_password
POSTGREST_JWT_SECRET=your_jwt_secret_minimum_32_characters_long_change_this
POSTGREST_PROXY_URI=http://localhost:3000

# CORS Configuration (for development - set to * to allow all origins)
CORS_ORIGINS=*

# pgAdmin Configuration (Optional)
PGADMIN_EMAIL=admin@traidenis.local
PGADMIN_PASSWORD=admin

# Application Configuration
VITE_POSTGREST_URL=http://localhost:3000
VITE_POSTGREST_ANON_KEY=anon

# Voiceflow (keep existing)
VITE_VOICEFLOW_API_KEY=your_voiceflow_api_key
VITE_VOICEFLOW_PROJECT_ID=your_voiceflow_project_id

# Anthropic SDK (keep existing)
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key

# Legacy Supabase (can be removed after migration)
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# N8N Webhooks (now stored in database)
# These are legacy and will be fetched from webhooks table
VITE_N8N_WEBHOOK_UPLOAD_NEW=http://localhost:5678/webhook/upload-new
VITE_N8N_WEBHOOK_FIND_SIMILAR=http://localhost:5678/webhook/find-similar
VITE_N8N_WEBHOOK_UPLOAD_SOLUTION=http://localhost:5678/webhook/upload-solution
