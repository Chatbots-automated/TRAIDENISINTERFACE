# =============================================================================
# TRAIDENIS INTERFACE — Environment Variables
# =============================================================================
#
# IMPORTANT: This application uses DIRECTUS API for all database operations.
# The API provider is Directus (https://directus.io), NOT Supabase, NOT Firebase.
# See src/lib/directus.ts for the client implementation.
#
# For production: set these in Netlify Environment Variables.
# For local dev: copy this file to .env and fill in the values.
# =============================================================================

# ---------------------------------------------------------------------------
# Directus API (primary database layer)
# ---------------------------------------------------------------------------
VITE_DIRECTUS_URL=https://sql.traidenis.org
VITE_DIRECTUS_TOKEN=your_directus_static_token_here

# ---------------------------------------------------------------------------
# Anthropic SDK (AI chat features — Claude)
# ---------------------------------------------------------------------------
VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here
